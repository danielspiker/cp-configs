eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
info:
  author: Daniel M. Nascimento
  github: https://github.com/danielspiker
  task: Checkpoint 1
server:
  port: ${PORT:9991}
message: Checkpoint 1
